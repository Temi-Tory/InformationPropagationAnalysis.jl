# API reference

The top-level `InformationPropagationAnalysis` module re-exports the five toolkit
submodules, the value types `Interval` and `pbox`, and one blessed entry point
per toolkit.

```@docs
InformationPropagationAnalysis.critical_path
```

For the full documented surface of each toolkit, see its page:

- [Input](@ref input)
- [Diamonds](@ref diamonds)
- [Probability](@ref probability)
- [CriticalPath](@ref criticalpath)
- [Flow](@ref flow)

## Index

```@index
```
